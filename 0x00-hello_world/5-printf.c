#include <stdio.h>

/**
 *main - it prints string with printf function
 *
 *Description: using the outcome
 *this program prints the string with proper grammar,
 *but the outcome is a piece of art
 *Return: always 0
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
